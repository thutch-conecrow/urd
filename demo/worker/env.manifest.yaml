# Background worker — shares DB and Stripe access with API, no Supabase auth needed
target: ".env"
vars:
  DATABASE_URL: supabase.database_url
  STRIPE_SECRET_KEY: stripe.secret_key
  STRIPE_WEBHOOK_SECRET: stripe.webhook_secret
