# Web frontend (Next.js) — only public/client-safe values
target: ".env.local"
vars:
  NEXT_PUBLIC_SUPABASE_URL: supabase.url
  NEXT_PUBLIC_SUPABASE_ANON_KEY: supabase.anon_key
  NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: stripe.publishable_key
  NEXT_PUBLIC_STRIPE_PRICE_PRO_MONTHLY: stripe.price_id.pro_monthly
